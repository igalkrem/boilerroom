# BoilerRoom — Project Structure

Source tree with per-file notes. Referenced from CLAUDE.md.


```
src/
├── proxy.ts                           # Next 16 proxy (was middleware.ts until 2026-08-05; NODE runtime only, Edge not available here). Matcher = all non-asset paths. Owns THREE things: the Content-Security-Policy incl. the per-request script nonce (moved here from next.config.mjs — headers there are static), the SEC-26 cross-site Origin check, and per-prefix rate limiting (BUCKETS)
├── app/
│   ├── (auth)/                        # Login & OAuth callback pages
│   ├── api/
│   │   ├── auth/                      # logout, refresh, session; google/{login,callback}; snapchat/{connect,callback,disconnect}; meta/{connect,callback,disconnect}; dev-login (local-only session bootstrap, 404s in prod)
│   │   ├── data/                      # GET/POST — reads/writes user-scoped JSON blobs for persistent metadata
│   │   ├── feed-providers/
│   │   │   └── channels/              # GET/POST/PATCH/DELETE — list, bulk-insert, force status (PATCH {id, newStatus}), hard-delete channels
│   │   │       ├── assign/            # POST — picks oldest available channel, marks in-use
│   │   │       ├── release/           # POST — moves in-use channel to cooldown
│   │   │       └── link-squad/        # PATCH {channelId, adSquadId, campaignSnapId?, platform?} — stores channel→ad_squad mapping for Predicto revenue JOIN. `platform` ("snap" | "meta", defaults to "snap") selects which API verifies the id — REQUIRED from the Meta orchestrator, which sends a Meta ad set id
│   │   ├── reporting/
│   │   │   ├── sync/                  # POST {adAccountId, startDate, endDate} — thin wrapper; delegates to syncAccount() in sync-logic.ts; maxDuration=300 (raised from 120 — squad-heavy accounts can spend up to ~30s in 429 backoff per squad when many accounts sync concurrently)
│   │   │   ├── meta-sync/             # POST {adAccountId, startDate, endDate, force?} — Meta stats sync; delegates to syncMetaAccount() in sync-logic.ts; maxDuration=300
│   │   │   ├── cron-sync/             # GET (Vercel Cron, :15 and :46 each hour) — classifies Snap accounts by network (getAccountNetwork), syncs Visymo accounts at :15 and Predicto accounts at :46; also syncs all Meta accounts (non-expired tokens) on every run; maxDuration=300
│   │   │   ├── sync-status/           # GET — returns per-feed {feedLastSynced, snapLastSynced, inSync} for Visymo and Predicto; infers account-to-network mapping via DB joins (visymo_report / feed_provider_channels)
│   │   │   ├── combined/              # GET ?adAccountId&startDate&endDate — dual query returning merged Snap+Meta metrics with EUR→USD conversion; accepts both Snap and Meta ad account IDs (checks isAdAccountAllowed OR isMetaAdAccountAllowed); Meta rows have platform:"meta", spend converted from cents; ad_squad_name resolved from DB column (no N+1 API calls)
│   │   │   └── drilldown/             # GET ?adSquadId&adAccountId&platform(snap|meta, default snap) — same dual Snap/Meta JOIN as combined but no date filter and no platform auto-detection (caller must pass platform); returns ALL available dates for one campaign
│   │   ├── silo/
│   │   │   ├── upload/                # Vercel Blob client-upload token endpoint (handleUpload)
│   │   │   ├── transcode/             # POST {blobUrl, fileName} — server-side H.264 transcode via @ffmpeg-installer/ffmpeg; downloads raw video from Blob → FFmpeg → re-uploads MP4 → returns {optimizedUrl}; maxDuration=300
│   │   │   └── delete/                # DELETE handler — removes blobs by URL array
│   │   ├── meta/
│   │   │   ├── ad-accounts/           # GET — lists Meta ad accounts; caches IDs in session.metaAllowedAdAccountIds + DB
│   │   │   ├── campaigns/             # GET (list) + POST (create) — Zod validation, auth + IDOR checks
│   │   │   ├── adsets/                # GET + POST + PATCH — full IDOR protection
│   │   │   ├── ads/                   # POST + PATCH — with IDOR guards
│   │   │   ├── creatives/             # POST — object_story_spec validation
│   │   │   ├── media/                 # POST — SSRF guard (.vercel-storage.com), discriminated IMAGE/VIDEO
│   │   │   ├── pages/                 # GET — list user's Facebook pages for object_story_spec
│   │   │   ├── page-ad-counts/        # POST {pageIds:string[], accountIds?:string[]} — running/in-review ad counts for ONLY the requested pages (a feed provider's allowedPageIds at launch). `accountIds` narrows the ads_volume sweep to a specific provider's ad accounts (intersected against session.metaAllowedAdAccountIds as an IDOR guard; falls back to the full owned list if the narrowed set is empty) — safe because a page belongs to exactly one feed provider (enforced in Traffic Sources), so that provider's accounts cover every account the page's ads could run through. Skips getBusinessPages() + name resolution. Returns {counts: Record<pageId, running>} (unseen page → 0). Used by WizardShell at launch instead of the heavier ad-limits GET
│   │   │   ├── ad-media/               # GET ?adIds=<comma-separated, up to MAX_ADS=20> — per ad: getAd → same IDOR check as `ads/route.ts` (isMetaAdAccountAllowed on ad.account_id); ads that 404 or fail the check are excluded (with a per-ad `{adId,error}` status) rather than failing the whole batch. Media across ALL accessible ads is resolved via `resolveBatchMedia()` (`lib/meta/media-download.ts`) — every image_hash/video_id location (object_story_spec, ad-level creative_asset_groups_spec, creative-level asset_feed_spec) is collected and **deduped globally by hash/video_id** before resolving, so the same asset reused across multiple ads (or multiple slots on one ad) is only resolved/downloaded once. Image hashes are grouped by their owning ad account (the `/adimages` lookup is account-scoped) and resolved one batched call per account; video ids are resolved in one single global batch (video-node lookups aren't account-scoped). Returns `{ads:[{adId,adName?,error?}], items:[{key,type,url,filename}], truncated, unresolvedCount}`. `&download=1` instead streams a zip (`jszip`, STORE compression, all in-memory — capped at `MAX_MEDIA_ITEMS=60` total across the whole batch) with `Content-Disposition: attachment`. Neither Graph API call (`GET /act_<id>/adimages?hashes=` for hash→url, `GET /?ids=<video_id,...>&fields=source` for video→url) existed anywhere in this codebase before — added specifically for this feature. `maxDuration=120` (heavier than the 60s norm; below the 300s tier reserved for transcode/sync). Powers `dashboard/ad-media/page.tsx`
│   │   │   └── ad-limits/             # GET — every FB Page across the user's Business Managers with name + Business Manager name (from getBusinessPages: /me/businesses → owned_pages/client_pages, needs business_management) + PAGE-LEVEL running/in-review count (ads_volume merged across all Meta ad accounts by actor_id). Returns {pages:[{pageId,name,businessName,running,instagramActorId}]}. Business list includes 0-ad pages (matches business.facebook.com/latest/ad_limits); ads_volume-only pages are unioned in and name-resolved via `?ids=` fallback. Facebook does NOT expose the numeric limit — client applies fixed 250. `instagramActorId` (each page's page-backed Instagram account, added 2026-07-16 for the Traffic Sources table's Instagram Actor ID column) is resolved via `resolveInstagramActorIds()` — checks the permanent global cache (`instagram-actor-cache.ts`) first, then calls `getOrCreatePageBackedInstagramAccount()` only for uncached pages, 5-way concurrent, and writes new results back to the cache. **Server-side cached indefinitely** (no TTL) per user in Vercel Blob (`metadata/{googleUserId}/meta_ad_limits_cache.json` via `src/lib/meta/ad-limits-cache.ts`) so the ~35 Graph calls (businesses+pages + 1 ads_volume per ad account) don't run on every load and trip the transient app rate limit (#4); cache is only refreshed on explicit user action (`?refresh=1` — the Refresh button on /dashboard/traffic-sources) or when no cache exists; pre-launch ad counts use the separate live `/api/meta/page-ad-counts` endpoint. If a fresh compute returns empty (rate-limited) or throws, the stale cache is served instead of an empty table
│   │   └── snapchat/
│   │       ├── campaigns/
│   │       ├── adsquads/
│   │       ├── creatives/
│   │       │   └── [id]/              # PATCH — fetches existing creative first, then PUT with preserved type/media fields + new web_view_properties.url (for {{ad.id}} injection after ad creation)
│   │       ├── creative-elements/     # POST — create Collection Ad product-tile placeholders (BUTTON/WEB_VIEW/DYNAMIC)
│   │       ├── interaction-zones/     # POST — create Collection Ad interaction zone grouping 4 creative elements
│   │       ├── ads/
│   │       ├── ad-accounts/
│   │       ├── profiles/              # GET ?adAccountId= → first profile_id for creative payload
│   │       └── media/                 # upload-init, upload-chunk, upload-finalize, upload (image + small video ≤4.4 MB), upload-from-blob (server fetches Blob → Snapchat, any size), poll, copy
│   └── dashboard/
│       ├── page.tsx                   # Campaign builder (WizardShell) — default landing page
│       ├── [adAccountId]/create/      # Campaign builder with pre-selected ad account
│       ├── create/                    # Campaign builder (no pre-selected account)
│       ├── pixels/                    # Snap Pixel CRUD UI (new/[id]/edit)
│       ├── meta-pixels/               # Meta Pixel CRUD UI (new/[id]/edit)
│       ├── presets/                   # Campaign preset CRUD UI (new/[id]/edit); list page is a horizontally-scrolling board of columns by feed provider (sorted by preset count), each split into a Snap sub-group and a Facebook sub-group (pill-tag divider); airy stat-row cards show Pixel/Bid/Budget/Device for both platforms (Facebook cards add a Geo group row above them) + Edit/Duplicate/Delete buttons; trailing self-stretch dashed "+ Add feed provider" button links to /dashboard/feed-providers; no "Load in Wizard"; "Country Groups" button opens CountryGroupsModal
│       ├── catalogue/                 # Image hosting for Snap product catalogue — upload images (JPEG/PNG/WebP/GIF, 20 MB max), table shows thumbnail + public URL (copy) + delete; data: localStorage + KV (boilerroom_catalogue_v1 / br_catalogue_v1); upload endpoint: /api/catalogue/upload (images only); delete endpoint: /api/catalogue/delete (ownership-verified against KV); sidebar nav item between Silo and Articles
│       ├── articles/                  # Article CRUD UI (new/[id]/edit); list page splits rows into an always-visible active table plus a "Paused articles (N)" section collapsed by default (`showPaused` state, `activeRows`/`pausedRows` derived from the same filtered+sorted array) — same collapsible pattern as Traffic Sources' "Hidden accounts"; the old status dropdown filter was removed since the split now replaces it; both tables render via a shared `renderArticlesTable(rows)` closure so Pause/Resume, Copy, Edit, Preview, Delete, and the expandable headline row stay in sync between sections
│       ├── feed-providers/            # Feed Provider board UI (card grid + FeedProviderModal) — own top-nav tab; each card's header is washed in the provider's color via `getFeedProviderWashClasses(provider.name)` (background-only sibling of `getFeedProviderBadgeClasses` in `lib/feed-providers.ts`, same Vizymo=orange/Predicto=blue/fallback=cyan hue table but without recoloring header text) and shows a name-colored pill (`getFeedProviderBadgeClasses`) plus Snap/Meta platform chips (Meta chip only shown when metaConfig has any accounts/pixels/pages/URL configured); stat grid adds a Pixels count (Snap + Meta combined) alongside Accounts/Domains/Base URL
│       ├── traffic-sources/           # Connections + ad account settings. Compact unified Snap+Meta ad-account table (platform icon | name/id | org | tz·currency | feed-provider chips | Active/Hidden toggle | ⋯); feed-provider chips (here, on the Facebook Pages table below, and on the pixel "Used By" columns) are colored per provider via `getFeedProviderBadgeClasses(name)` in `lib/feed-providers.ts` — name-keyed lookup (case-insensitive), currently Vizymo=orange, Predicto=blue, any other/future provider name falls back to the original cyan; account search + platform filter pills (All/Snap/Meta) + a "⬇ Export CSV" button (added 2026-07-27) that always exports every account (Platform, Name, Account ID, Business Manager/Org, Timezone, Currency, Status) regardless of the current search/platform filter or hidden state — a full-inventory export, not a "download what's on screen" one, built client-side from the already-loaded `unifiedAccounts`/`isAccountHidden()` (same Blob+`<a download>` CSV pattern as `PerformanceTable.tsx`); clicking the feed-provider cell / ⋯ opens an inline checklist row to assign providers. **Active accounts fill the main table; hidden accounts collapse into a "Hidden accounts (N)" expandable table below** (hiddenTotal counts all hidden regardless of search; toggling Status live-moves a row between the two tables). Connected Sources rendered as two compact cards (Snapchat/Meta connect·disconnect + Meta token-expiry warning). Snap Pixels + Meta Pixels rendered as compact tables (Add/Edit/Delete, "Used By" provider chips). Fully dark-mode styled. Hidden state persists in adAccountConfigs (loadAdAccountConfigs); toggling hide/assign calls syncFeedProviderAssignments to keep provider allowedAdAccountIds in sync. **Facebook Pages table** (own section, shown when Meta connected): lists FB pages from useMetaAdLimits (`/api/meta/ad-limits`, sourced from ads_volume — NOT /me/accounts, which is often empty) with ad-limit status — columns Page (name + id) | Business (owning Business Manager name) | Ads (usage bar `running / 250`, green fill, red when full) | feed-provider chips | Active/Hidden toggle | ⋯; the limit is a fixed 250 (Facebook's page ad limit, not customizable); **sorted most-occupied first** (running desc, like business.facebook.com/latest/ad_limits); search matches page name/id/business; a ↻ Refresh button forces a live recompute (`?refresh=1`, the only way to refresh — no automatic TTL); same active-table + collapsed "Hidden pages (N)" split. Page config (hidden + feedProviderIds) persists in pageConfigs (loadPageConfigs/upsertPageConfig, key boilerroom_page_configs_v1 / br_page_configs_v1). **A page can be assigned to at most ONE feed provider** (pill-button single-select in the expand row, not checkboxes — `setPageFeedProvider(pageId, providerId)` clicking the already-assigned provider clears it) because its running-ad count is one global number; splitting a page across providers would make "most ads remaining" and the page-ad-counts account-scoping both unreliable. Assigning a page calls syncProviderPageAssignments which writes provider.metaConfig.allowedPageIds AND keeps metaConfig.pageId = allowedPageIds[0]. At launch, WizardShell collects the batch's providers' allowedPageIds AND allowedAdAccountIds, POSTs both to /api/meta/page-ad-counts (accountIds narrows the ads_volume sweep — safe under the one-provider-per-page invariant), and pickBestPage() (synthesize-campaign.ts) selects the assigned page with the most ads remaining (ties → first); MetaTab's page selector is now a read-only assigned-pages summary (no longer authors pageId)
│       ├── build-log/                 # Build session history — timeline feed; each launch session is a collapsible card with colored dot (green/amber/red); timeline column shows date+time on one line to the left of the dot — date rendered as a blue accent pill badge (more prominent than the plain-text time next to it), shown only on the first session of each calendar day (blank for consecutive same-day sessions) so the day, not the time, is the visual anchor; per-squad table: Time | Name | Status | Budget | Bid | Actions; inline Budget/Bid edit via PATCH /api/snapchat/adsquads; Pause/Activate toggle + Delete (calls DELETE /api/snapchat/adsquads, marks squad DELETED locally); persisted via localStorage + KV (br_build_log); WizardShell appends a session after each launch
│       ├── performance/               # **Default landing page** — loads Snap + Meta rows from DB immediately on mount; syncAndReload fires both /api/reporting/sync (Snap) and /api/reporting/meta-sync (Meta) in parallel; SyncStatusBar (right-aligned on the date-picker/Clear-filters toolbar row) shows per-feed sync status with subtle icon-only Force Refresh; loadSquadDetails fetches from both /api/snapchat/adsquads and /api/meta/adsets, converts Meta cents→micro for unified SquadDetail map; cron keeps DB fresh. The mount-load effect gates on both `useAdAccounts`'s and `useMetaAdAccounts`'s own `isLoading` (not just "accounts list is non-empty") before firing its one-shot `didLoad` load — the two hooks are independent SWR calls with independent latency, so gating on either list alone raced whichever platform resolved first and silently skipped the other for that page load (fixed 2026-07-28)
│       ├── ad-media/                  # "Ad Media Downloader" — first-class sidebar tab (not a debug page). Textarea accepts Ad IDs one per line and/or comma-separated on a line (split on `/[\r\n,]+/`, deduped client-side) → fetches /api/meta/ad-media?adIds=<comma-joined> → shows per-ad found/error status + a grid of resolved UNIQUE media across the whole batch (type badge, image thumbnail, filename) → "Download All (N)" is a plain `<a href=".../ad-media?adIds=&download=1" download>` relying on the route's `Content-Disposition` header (simpler/more reliable than a client Blob dance for cross-origin Facebook CDN media, and matches the existing `<a download>` pattern in `AssetPreviewModal.tsx`)
│       ├── silo/                      # Media library
│           ├── page.tsx               # Library grid with search/filter/delete; auto-fill grid (minmax 180–240px) keeps cards compact on wide screens
│           ├── upload/                # Upload page with tag selector + SiloUploader
│           └── tags/                  # Tag CRUD (create, edit, delete)
│   └── privacy/                       # Privacy policy page (required by Meta app review)
├── components/
│   ├── wizard/
│   │   ├── CampaignCanvas.tsx         # React Flow free-form canvas; grey bg; fitView maxZoom 0.75
│   │   ├── CanvasControls.tsx         # Top bar: Auto-align (manual re-trigger), Review →; computeAutoLayout (dagre LR, ranksep 200) with post-processing sort to match upstream y-order; auto-layout fires automatically on every structure change via useEffect in CampaignCanvas; exports NODE_WIDTH/NODE_HEIGHT/GROUP_CARD_H/ARTICLE_EXPANDED_H for reuse by LaneOverlay's bounding-box math (GROUP_CARD_W is dagre-sizing only now — group/row nodes are excluded from LaneOverlay's x-range entirely)
│   │   ├── LaneOverlay.tsx            # Decorative provider swim-lane overlay — child of <ReactFlow>, uses useViewport() to convert flow coords to screen space; draws a translucent band + label per provider (bounding box over that provider's rows/provider/ts/router/articles/accounts/presets), a dashed lane divider between adjacent providers (opacity fades 0%→12%→0% via SVG linearGradient), a short vertical divider between a provider's two active Traffic Source siblings, and a plain (non-fading) dashed "Unassigned" divider + label per lane/column/platform within a lane's account/preset column marking the seam between that platform's connected nodes and its not-yet-wired ones (`LaneBound.orphanDividers`, computed in `laneBounds`; one per Snap/Meta half, not shared across both). Rendered outside the `nodes` array so it can never affect fitView's bounding-box calculation
│   │   ├── nodes/
│   │   │   ├── CreativeGroupNode.tsx  # Row node: horizontal row of portrait cards; always-visible side dock (⊞ Add slot, 🔗 Provider, ↓ New row, ⧉ Duplicate, ✕ Remove row) sits inside nodeWidth between cards and handle; handle anchored at nodeWidth right edge (past dock); each slot is a flex-col: card + "＋ Add creative" button below; multi-creative slots show stacked colour-coded name pills; up to 8 slots, 5 creatives/slot
│   │   │   ├── ProviderNode.tsx       # Left accent bar + row count + Snap/Meta toggle buttons (visible when connected); hidden until first row exists; no "+ Router" button; no article-picking affordance (moved to TrafficSourceNode); toggling a platform spawns/removes that platform's TrafficSourceNode
│   │   │   ├── TrafficSourceNode.tsx  # One per (connected provider, active platform) — up to two independent sibling nodes per provider, id `ts-{feedProviderId}-{platform}`, sits between Provider and Article/Router; single-platform label ("Snap"/"Meta") + always-visible "＋ Pick articles" button (no gate — the node's existence is the platform selection); "in" handle click removes this platform (routes through `toggleProviderTrafficSource`, same as the Provider card's toggle button)
│   │   │   ├── RouterNode.tsx         # Sleek circle (⑃ icon) — auto-inserted when provider gets second article
│   │   │   ├── ArticleNode.tsx        # One node per providerToArticle edge (platform-specific, `data.platform`) — Snap/Meta badge next to the slug, same pattern as AdAccountNode/PresetNode; full slug (break-words, no truncation) + inline headline editor (expand ▼ button, no page icon); expanded panel uses provider-color border+bg tint, uppercase tracked labels, translucent dark fields; no query subtext. No CTA picker — call_to_action is fixed per platform (CTA is fixed per platform — see docs/snapchat-api.md)
│   │   │   ├── AdAccountNode.tsx      # Initials avatar + account name (no ID shown); connected state from articleToAdAccount edges (no click-select); dashed/dimmed border + italic "unassigned" sub-label when disconnected; exports `SnapGhost` (platform badge icon, reused by PresetNode)
│   │   │   └── PresetNode.tsx         # Name + config + duplication rows with full article slugs (break-words); no Creatives/set control (replaced by groups); traffic-source logo badge (blue "f" / yellow SnapGhost) next to the name; dashed/dimmed border + italic "unassigned" sub-label when disconnected (no articleToPreset edge)
│   │   ├── edges/
│   │   │   └── ProviderEdge.tsx       # Glow-halo orthogonal routing — two layered SVG paths (wide blurred halo + crisp stroke) in provider color; no dashed pattern. Path is a single right-angle turn per edge (`getOrthogonalPath`) — straight out of the source, one rounded corner-pair (radius 10, capped by available space) down/up to the target's row, straight into the target; a plain straight line when source/target share a y. Edges always flow left-to-right column-wise so x never moves backward — replaces the earlier cubic-bezier midpoint S-curve
│   │   ├── ReviewAndPost.tsx          # Fallback campaign name template + launch matrix table; shows "Provider template active" badge when any provider has a naming template
│   │   ├── WizardShell.tsx            # Build/Review/Done mode toggle + sequential launch loop
│   │   ├── SubmissionProgress.tsx
│   │   └── LoadPresetBanner.tsx
│   ├── feed-providers/
│   │   ├── FeedProviderModal.tsx      # Large modal (max-w-3xl) with 2 traffic-source tabs: Snap | Facebook. Each tab is a scrollable stack containing that source's revenue source, ad accounts, pixels, URL params, naming template, channels, and domains (Channels/Domains are no longer standalone tabs)
│   │   ├── NamingTemplateEditor.tsx   # Shared segment-pill naming editor (exports NAMING_MACROS, resolveNamingPreview); theme prop "violet" (Snap) | "blue" (Meta)
│   │   └── tabs/
│   │       ├── SnapTab.tsx            # Full Snap-source config stack: revenue source, assigned ad accounts, allowed pixels, URL Parameters (snapConfig.urlConfig), Campaign Naming Template (shared NamingTemplateEditor, violet), Channels (ChannelsTab trafficSource="Snap"), Domains (DomainsTab trafficSource="Snap"); takes domains + onDomainsChange + feedProviderId
│   │       ├── UrlParametersTab.tsx   # Parameter rows, always-visible filtered macro chips (two groups: {platform} Native / BoilerRoom), live preview; hideBaseUrl + platform ("snap"|"meta") props; typed on UrlConfig
│   │       ├── ChannelsTab.tsx        # CSV upload + manual textarea entry; scoped to a trafficSource prop ("Snap"|"Meta") — GET filters by source, inserts tagged with it; dedup, status table, lifecycle controls
│   │       ├── DomainsTab.tsx         # Domains filtered to a trafficSource prop; per-domain Snap/Facebook source chips (a domain can be shared); operates on the full shared domains list
│   │       └── MetaTab.tsx           # Full parity with Snap: revenue source (Predicto FB), **Dashboard ROAS Display** divisor input (`metaConfig.roasDisplayDivisor`, default 1) — correction for providers whose ad sets report an inflated ROAS floor. NOT display-only despite the name: the read side divides (`/100/divisor`) and the **save side multiplies it back in** (`Math.round(pct * divisor * 100)`, `PerformanceTable.tsx:819`), so it is a factor in the integer Meta stores as `roas_average_floor`. It round-trips as an identity, which is the point — it adapts the floor to that provider's pixel value scale (Predicto's pixel reports ~$37/conversion vs ~$0.11 elsewhere, so `900000` and `9000` are both break-even) — but when auditing a surprising `roas_average_floor`, this divisor is part of the arithmetic. **Do not set it to 1 to "fix" inflated floors** — see the Security/data note on the two coexisting scales. The launcher also applies it to preset `roasFloor` via `toRoasAverageFloorForProvider()`. Consumed by PerformanceTable's Bid column, assigned ad accounts, allowed Meta pixels (loadMetaPixels), URL Parameters (metaConfig.urlConfig, platform="meta"), Campaign Naming Template (shared editor, blue), Associated Facebook Pages (names via /api/meta/ad-limits), Channels (trafficSource="Meta"), Domains (trafficSource="Meta")
│   ├── silo/
│   │   ├── SiloUploader.tsx           # Batch uploader: hash → optimize → Blob upload (3 concurrent)
│   │   ├── SiloBrowser.tsx            # Picker modal for canvas wizard integration; same TagFilterBar toolbar as the main Silo page
│   │   ├── TagFilterBar.tsx           # Segmented tag-filter toolbar (All + one pill per tag, always-visible counts, deterministic per-tag color via getTagColorClasses) — shared by silo/page.tsx and SiloBrowser.tsx, replaces the old "All tags" <select>
│   │   ├── AssetCard.tsx              # Thumbnail card with quick actions; bulk-mode checkbox overlay; single "Snap ✓" badge + single "Meta ✓" badge; portrait preview capped at max-h-[280px]; amber ⚠ "Re-upload for Snap" badge on VIDEO assets with no optimizedUrl (uploaded before H.264 pipeline)
│   │   ├── AssetPreviewModal.tsx      # Full preview + metadata + usage history
│   │   ├── SnapchatUploadModal.tsx    # Pre-upload to Snapchat — accepts assets: SiloAsset[] (single or bulk); 2-concurrent per asset
│   │   └── MetaUploadModal.tsx        # Pre-upload to Meta's media library — mirrors SnapchatUploadModal (assets: SiloAsset[], single/bulk, 2-concurrent) but no cross-account media-copy step (Meta has none) and no client-side polling (POST /api/meta/media already resolves image hash / polls video processing server-side); stages: queued → uploading → ready | failed
│   ├── layout/
│   │   ├── AuthGuard.tsx
│   │   ├── Sidebar.tsx                # Left sidebar navigation; collapsible toggle (double-chevron button); width w-56 expanded / w-12 collapsed; state persisted in localStorage under `br_sidebar_collapsed`
│   │   ├── TopBar.tsx                 # Top bar (page header area)
│   │   └── KVHydrationProvider.tsx    # On dashboard mount: hydrates localStorage from Vercel Blob; blocks render on fresh session until data loaded
│   ├── performance/
│   │   ├── PerformanceSummaryTables.tsx # Five compact summary tables (By Article, By Feed, By Traffic Source, By Feed × Traffic Source, By Date) rendered between KpiSummaryBar and PerformanceTable, laid out on a centered CSS grid (`max-w-[1400px] mx-auto grid grid-cols-12 gap-4` — Article `col-span-12` full-width row on its own since it has the most columns, Feed and Traffic Source `col-span-6` each sharing the next row, Feed × Traffic Source `col-span-7` and Date `col-span-5` sharing the last) instead of the old fixed-pixel-width flex-wrap rows; each table's own wrapper also has `overflow-x-auto` as a fallback so columns scroll instead of getting silently clipped on narrower viewports; each grid-item wrapper and the RoiTable/DateTable root both carry `h-full` (with the scrollable content area `flex-1`) so cards sharing a grid row stretch to equal height regardless of row count; the whole section sits behind a collapsible toggle bar (`collapsed` state, **defaults collapsed** — was open) — collapsing hides only these five cards, KpiSummaryBar and PerformanceTable (rendered by performance/page.tsx, not this component) stay visible either way; By Article and By Feed × Traffic Source render every row with no internal scroll; By Date is the only table that still scrolls internally (`overflow-auto max-h-64`, sticky header retained) since it alone can have dozens of rows (last 30 days); By Article, By Feed, By Traffic Source, By Feed × Traffic Source: Cost/Revenue/Profit/ROI + 1D-ago/2D-ago historical ROI + Live campaign count (ACTIVE squads cross-referenced from squadDetails prop); By Date: Cost/Revenue/Profit/ROI + Live count; all metric columns sortable (click → desc, click again → asc, null ROI sorts last); provider color coding: 3px left border on first td using PROVIDER_COLORS palette keyed by provider createdAt sort order (same as canvas wizard) — By Traffic Source rows use PLATFORM_COLORS for the same border instead, since there's no provider concept there; **By Article's Feed column now renders a real colored pill** via `getFeedProviderBadgeClasses()` (`src/lib/feed-providers.ts`, name-keyed — same badge used on Feed Providers/Traffic Sources pages) instead of plain gray text; **By Traffic Source's label cell shows the platform icon inline** via RoiTable's new `showPlatformIcon` prop (reuses `PlatformIcon` from PerformanceTable.tsx); only By Date has a Total row now — By Article, By Feed, By Traffic Source, and By Feed × Traffic Source all render with `showTotal={false}`; By Feed × Traffic Source groups by `${resolveProviderKey()}|||${row.platform}` and renders the platform via RoiTable's `showSource` column; By Traffic Source groups by `row.platform` alone; By Date always shows last 30 days (last30Rows prop, independent of date picker); accepts squadDetails?: Map<string, SquadDetail> passed from performance/page.tsx; loads articles+providers from localStorage; feed provider resolved via `resolveProviderKey()` (`src/lib/reporting/provider-key.ts`, shared by this component and PerformanceTable — NOT a separate copy), a three-tier fallback: (1) feed_provider_id from DB, (2) domain_name matched against provider.domains[].baseDomain (Meta rows always have domain_name="" from the combined route, so this tier never fires for Meta), (3) ad_account_id matched against provider.snapConfig.allowedAdAccountIds OR provider.metaConfig.allowedAdAccountIds; truly unmatched → "Unknown". Tier 3 previously checked only snapConfig, so a Meta campaign on an account assigned solely via metaConfig (no channel link, no domain match) fell through to "Unknown" even though the account was correctly configured — fixed 2026-07-28; CombinedRow now includes ad_account_id (added to combined and drilldown SQL SELECT); **Clickable rows — now all four grouping tables, not just Article/Feed**: a single `selection: { type: "article"|"feed"|"source"|"feedSource"; key } | null` state (replaced the old separate `selectedArticleKey`/`selectedFeedKey` states) drives mutually-exclusive row selection across By Article, By Feed, By Traffic Source, and By Feed × Traffic Source — clicking a row in any of the four highlights it (blue tint + bold), clears any other table's selection, and fires `onFilterChange({ squadIds: Set<string>, label })` up to page.tsx; page.tsx stores summaryFilter and computes tableRows (rows filtered by squad ID set) passed to PerformanceTable, whose own `onFilteredRowsChange` feeds KpiSummaryBar — so the KPI tickets and the table below react to whichever of the four is selected too; By Date remains the only passive table (no onRowClick — cannot itself trigger a filter) but its rows still recompute from `filteredLast30Rows` so it reflects whatever the other four have selected; By Article always uses unfiltered rows (all articles stay visible/clickable) — the other three (Feed, Traffic Source, Feed × Traffic Source) recompute from `internalFilteredRows`, which now branches on `selection.type` (article/feed/source/feedSource) via a shared `applySelectionFilter()` helper; clicking the same row again deselects; a "Filtered by: X ×" chip appears above the grid; changing the date range clears the filter
│   │   ├── PerformanceTable.tsx       # Meta Ads Manager-style table: toolbar (Edit/Delete/Search/Columns/CSV) with an always-visible filter bar below it (no more click-to-reveal toggle — `showFilters` state removed), grouped into four labeled pill clusters (Scope: Article + Feed Provider multi-selects; Source: Snap/Meta segmented toggle driving `filterPlatforms`, filters on `r.platform`, uses the same `PlatformIcon`; Status: Active/Paused; Metrics: metric filter builder), "Clear all" pinned right when any filter is active; bulk edit panel, 30 optional metric cols (includes Last Change — on by default), delivery badge, inline Budget/Bid/Status editing — **the Bid cell is bid-strategy-aware**: a small colored pill names the row's `SquadDetail.bid_strategy` (Snap: TARGET COST green / AUTO BID sky / MAX BID cyan; Meta: ROAS purple / COST CAP cyan / LOWEST COST gray, via `BID_STRATEGY_PILLS`), `NO_BID_TARGET_STRATEGIES` (AUTO_BID, LOWEST_COST_WITHOUT_CAP) show the pill + a dash since those strategies have no user-set bid; Meta ad sets on `LOWEST_COST_WITH_MIN_ROAS` show/edit their ROAS target as a **percentage** (`SquadDetail.roas_average_floor / 100`, rounded) via the same click-to-edit pencil pattern as Budget/Bid (`editingRoas`/`roasDraft`/`saveRoas`), PATCHing `/api/meta/adsets` with `bid_strategy: "LOWEST_COST_WITH_MIN_ROAS"` + `bid_constraints.roas_average_floor` (percent × 100 — Meta stores the floor at ×10000 scale, e.g. 90% → 9000); the displayed/edited percentage is additionally divided (and, on save, multiplied back) by `provider.metaConfig.roasDisplayDivisor` (`getRoasDivisor(row)`/`getRoasDivisorBySquadId`, resolved via `resolveProviderKey`) — a per-provider adaptation to that provider's pixel value scale (Predicto's pixel reports ~$37/conversion vs ~$0.11 elsewhere), set in the Feed Provider Meta tab. **NOT display-only and it DOES change the value sent to Meta** — `saveRoas` multiplies it back in, so a cell reading 90% at divisor 100 writes `900000`; all other strategies keep the original dollar bid editor — sort arrows, click name → DrilldownModal; shift+click checkbox range selection (lastCheckedIdx ref); changelog recording: saveBudget/saveBid/toggleStatus/applyBulk all call addChangeEntry() on success (applyBulk captures old/new values inside the Promise.allSettled map and carries them on the return value); ColumnSelector lives in toolbar; **platform-aware icon** left of campaign name (yellow Snap ghost for Snap, blue "f" badge for Meta) — **clickable**: `buildTrafficSourceUrl(row)` opens the traffic source's Ads Manager in a new tab (Snap → `https://ads.snapchat.com/{ad_account_id}/manage` account-scoped, since a squad-specific link needs an opaque UI-generated `manageViewId`; Meta → `https://adsmanager.facebook.com/adsmanager/manage/adsets?act={bareNumericAccount}&business_id={bm}&selected_campaign_ids={campaign_id}&selected_adset_ids={ad_set_id}&nav_source=no_referrer` — `business_id` (from `MetaAdAccount.business.id`) and `campaign_id` (from `/api/meta/adsets`) are carried into the `SquadDetail` map (`SquadDetail.business_id` / `.campaign_id`) and read via the row's `detail`; both are optional — if squad details haven't loaded yet the link degrades to just `act` + `selected_adset_ids` + `nav_source`); `buildTrafficSourceUrl(row, detail?)`; `stopPropagation` prevents also opening the drilldown; totals row / rows missing an account id stay non-clickable; inline edit PATCH routes to /api/snapchat/adsquads or /api/meta/adsets based on row.platform; CombinedRow.platform and AggrRow.platform track "snap"|"meta"; clipboard copy icon on hover copies the campaign name (green checkmark flash for 1.5s); Article + Provider filter dropdowns each have a live search box at the top; bulk-edit Budget/Bid cards each have a 3-tab mode selector ($ Set / +/- $ / +/- %) with a green/red direction toggle (+/−) for non-Set modes; bulk edit panel also has an **Ignore card** (amber-themed) — `hideSelected()` adds every selected `squadId` to `hiddenSquadIds` in one shot (same `br_perf_hidden_squads` localStorage key the single-row DrilldownModal toggle uses), then clears selection and closes the panel; each row's provider color (from `providerColorMap`/`PROVIDER_COLORS`) renders as two layered inset box-shadows on the `<tr>` — a 3px left-edge stripe plus a full-row 6%-opacity wash (`hexToRgba(stripeColor, 0.06)`) via `inset 0 0 0 9999px`, so hover/selection background classes still show through underneath
│   │   ├── KpiSummaryBar.tsx          # Horizontal KPI strip (8 cards: Spend, Revenue, ROI, Profit, Impressions, Clicks, Funnel Clicks, CTR); sums raw CombinedRow[]; loading skeleton; ROI card tinted green/amber/red
│   │   ├── DrilldownModal.tsx         # Per-ad-squad daily breakdown — requires a `platform` prop ("snap"|"meta", passed from PerformanceTable's `drilldown` state / `r.platform`) forwarded as a query param so /api/reporting/drilldown queries the right table (meta_ad_set_stats vs snapchat_ad_squad_stats) — omitting/mismatching it silently returns 0 rows ("No data available"); async-fetches ALL dates via /api/reporting/drilldown (no date filter); two-tab layout: "Daily Breakdown" (existing table) and "History" (change log for this campaign from getEntriesForSquad(); logVersion counter forces re-read after inline edits within the modal); modal header has "Columns" button (ColumnSelector, 26 metrics, br_drilldown_cols / br_drilldown_cols_order) and an eye-slash "Ignore campaign" button (accepts isHidden + onToggleHide props from PerformanceTable); derive() computes profit/rpr/cpr/rpc/cpm/cpc/cvr/ctr/fill_rate from raw CombinedRow fields; DrilldownModal inline Budget/Bid/Status saves also call addChangeEntry() + increment logVersion so the History tab refreshes immediately; clicking the `bg-black/60` backdrop closes the modal (`onClose` on the outer div), the inner card stops click propagation so interacting with the modal itself doesn't close it
│   │   ├── DateRangePicker.tsx        # Google Ads-style date picker: presets left, two-month calendar right; default = Today
│   │   └── ColumnSelector.tsx         # Dropdown checklist to show/hide metric columns; 30 columns (raw + computed + snap_results/snap_cost_per_result/snap_purchase_value_usd + last_change); persists to localStorage (br_perf_cols); loadSavedColumns() auto-adds any DEFAULT_VISIBLE_COLUMNS key absent from the saved order (uses saved order as "known at save time" baseline — new default columns appear for existing users without overriding deliberate hide choices); accepts optional columns/storageKey/orderStorageKey props to reuse in other contexts (e.g. DrilldownModal uses br_drilldown_cols)
│   ├── ui/
│   │   └── MultiSelect.tsx            # Controlled multi-select dropdown with checkboxes (react-hook-form Controller); popover renders via a portal into document.body with position:fixed (computed from the trigger button's getBoundingClientRect, resynced on scroll/resize while open) so it isn't clipped by a scrollable ancestor (e.g. a modal's overflow-y-auto content area); option list capped at max-h-72 with its own scroll; Select all / Clear all buttons at the top of the popover
│   ├── pixels/                        # PixelForm + MetaPixelForm components
│   ├── presets/                       # PresetForm — flat single-column form; Traffic Source selector (Snap/Facebook); Meta shows Pixel (from loadMetaPixels(), not Snap pixels), Bidding Strategy (Maximize conversions/value + optional Cost per Result / ROAS goal), billing event, Daily Budget (USD), publisher platforms; Snap-specific fields hidden for Meta; Country Group selector above Geo Targeting (read-only display + unlink when linked). No Call to Action field — CTA is fixed per platform, not configurable (CTA is fixed per platform — see docs/snapchat-api.md). CountryGroupsModal.tsx — list/create/edit/delete Country Groups (mirrors FeedProviderModal shell)
│   └── articles/                      # ArticleForm component
├── hooks/
│   ├── useCanvasStore.ts              # Zustand store for canvas wizard graph state + buildCampaignMatrix()
│   ├── useMetaPages.ts                # SWR /api/meta/pages → MetaPage[] (/me/accounts; often empty — table uses useMetaAdLimits instead)
│   ├── useMetaAdLimits.ts             # SWR /api/meta/ad-limits → { pages: {pageId,name,businessName,running}[], runningByPage, cachedAt, refresh() } — page list source for the FB Pages table (businesses + ads_volume, server-cached; refresh() bypasses cache)
│   └── useWizardStore.ts              # Legacy Zustand store (still used by LoadPresetBanner + preset/use page)
├── lib/
│   ├── snapchat/                      # Server-side API client (campaigns, adsquads, creatives, collection, media, profiles, auth, stats); collection.ts = createCreativeElements + createInteractionZones (Collection Ads); errors.ts = isEntityNotFound(err) — separates "upstream says no such entity" (reject) from "upstream was unreachable" (retry) in parent-id verification
│   ├── meta/                          # Meta Graph API client library (version pinned in graph-version.ts): client.ts (metaFetch, Bearer auth, no token refresh), errors.ts = isMetaEntityNotFound(err) — Meta counterpart to snapchat/errors.ts; matches `Meta API error 404` plus Graph's "does not exist"/"Unsupported get request" 400s, so a transient 5xx is not mistaken for a deleted entity, campaigns.ts, adsets.ts (getAdSet/getAdSets both request `effective_status`), creatives.ts (uploadImage/uploadVideo/pollVideoStatus; re-exports buildAdvantagePlusCreativeFeatures from creative-features.ts), creative-features.ts (buildAdvantagePlusCreativeFeatures(mediaType) — pure/dependency-free builder for degrees_of_freedom_spec.creative_features_spec, deliberately kept free of metaFetch/session imports so meta-submission-orchestrator.ts, which runs client-side, can import it directly without pulling server-only code into the browser bundle), ads.ts, stats.ts (Insights API), pages.ts (getPages), adaccounts.ts, ad-volume.ts (getAdsVolume — ads_volume per page; exports DEFAULT_PAGE_AD_LIMIT=250), business-pages.ts (getBusinessPages — /me/businesses → owned_pages/client_pages → {pageId: {name, businessName}}; getOrCreatePageBackedInstagramAccount — GET/POST /{pageId}/page_backed_instagram_accounts, requires pages_read_engagement scope; isInstagramActorUsableByAdAccount — checks a PBIA against a specific ad account's own /instagram_accounts list), ad-limits-cache.ts (readAdLimitsCache/writeAdLimitsCache — indefinite per-user Blob cache for the ad-limits result, refreshed only on explicit user action), instagram-actor-cache.ts (getCachedInstagramActorId/setCachedInstagramActorId — global, non-expiring Blob cache keyed by pageId; a page's PBIA never changes), media-download.ts (resolveAdMediaRefs/fetchImageUrls/fetchVideoUrls/resolveAdMedia — pure resolution logic for the Ad Media Downloader tab: walks every hash/video_id location on an ad+creative, dedupes, then resolves each to a real URL via `/adimages?hashes=` and `/?ids=&fields=source`) — all with IDOR guards on mutations
│   ├── submission-orchestrator.ts     # Snapchat: uploadMedia → channel assign → campaigns → adSquads → URL resolve → creatives → ads → patchCreatives
│   ├── meta-submission-orchestrator.ts # Meta: uploadMedia (2-concurrent) → channelAssign → campaigns → adSets → creatives+ads (individual creates, no batch). Every ad payload also carries `creative_asset_groups_spec` (see "Flexible" ad format note below) — this is what drives the "Flexible" label, separate from `degrees_of_freedom_spec` on the creative. Stage 5 resolves all of `synthesis.creatives` first, then branches: a single-media-item creative group (the common case) still gets its own one-creative/one-ad path, reusing that ad's own image/video in `creative_asset_groups_spec`; a 2+ item creative group (from the wizard's "＋ Add creative" grouping, up to 5 media items — see `CreativeGroup` in `types/wizard.ts`) instead gets bundled into exactly ONE creative + ONE ad, with every item's image/video hash folded into a single `creative_asset_groups_spec.groups[0]` (`images: [...]`/`videos: [...]` arrays) — confirmed 2026-07-20 this is the desired Meta behavior for multi-creative groups (Snap keeps its existing one-ad-per-media-item-under-one-ad-squad behavior for groups instead, unchanged in `submission-orchestrator.ts`). Only one `results.creatives`/`results.ads` entry is pushed per bundled group-ad (not one per item) so `WizardShell.tsx`'s build-log counts and `SubmissionProgress.tsx`'s success totals — which count array entries, not distinct `platformId`s — don't double-count. uploadMedia first checks each creative's metaImageHash/metaVideoId (set at synthesis from a Silo pre-upload) and populates mediaMap directly without calling POST /api/meta/media — only creatives lacking a cached ref go through the upload queue. **After a successful upload, `uploadWorker()` now also calls `updateMetaUpload(creative.siloAssetId, adAccountId, { stage: "ready", imageHash|videoId })`** (`lib/silo.ts`) to persist the ref back onto the Silo asset — confirmed live 2026-07-21 via Vercel logs that this was missing: 4 separate launches of the same article/asset in one session each did a full fresh `POST /api/meta/media` upload (distinct video IDs every time) because only the manual Silo "→ Meta" pre-upload modal (`MetaUploadModal.tsx`) ever wrote to `metaUploads[]` — a real launch's upload was discarded from the cache the moment it finished, even though `getMetaMediaRef()`'s read side was already correct. Ad set payload conditionally sets bid_strategy/bid_amount/bid_constraints — omitted entirely for the default LOWEST_COST_WITHOUT_CAP; COST_CAP sends bid_amount only when bidAmountCents is truthy; LOWEST_COST_WITH_MIN_ROAS sends `bid_constraints: { roas_average_floor: roasFloor * 10000 }` (NOT bid_amount) only when roasFloor is truthy — confirmed live 2026-07-15 via GET /api/meta/adsets with `bid_constraints` added to the fields list: the reference "boiler" ad set stores its 0.9 ROAS goal as exactly `{"roas_average_floor":9000}`. Two earlier attempts sent the value through `bid_amount` instead (at *1000 and *10000 scale) and both failed with "Bid Strategy Doesn't Support Value Optimization" (error_subcode 1885324) — the scale was already right on the second attempt, the field name was wrong. Campaign payload always sends `is_adset_budget_sharing_enabled: false` — Meta requires this field explicitly (confirmed live 2026-07-15, error_subcode 4834011) whenever the campaign has no daily_budget/lifetime_budget of its own, which this app never sets (budget always lives on the ad set — ABO). `/api/meta/campaigns`'s Zod schema has no `.passthrough()`, so this field must be listed there too or it gets silently stripped before reaching Meta. `age_min`/`age_max` are only sent when the preset explicitly sets them (no enforced floor) — omitting `age_min` lets Meta implicitly include under-18 (under-20 in Thailand) audiences, which Meta rejects alongside manual `publisher_platforms` selection ("Selected targeting options unavailable for young people", error_subcode 1870249, confirmed live 2026-07-15). An earlier attempt force-set a floor of 18/20 to work around this; reverted at the user's request — Thailand must be excluded from the preset's geo list manually instead when `publisher_platforms` is used. Video creatives require a real thumbnail — Meta rejects `video_data` with no `image_hash`/`image_url` at all ("Your ad needs a video thumbnail", error_subcode 1443226, confirmed live 2026-07-15; an empty `image_hash: ""` is rejected too, so `/api/meta/creatives`'s Zod schema makes both `image_hash` and `image_url` optional rather than requiring one specific empty-string shape). Before building a video creative, the orchestrator calls `GET /api/meta/media?videoId=` (added for this) to fetch Meta's auto-generated thumbnail (via `getVideoThumbnailUrl()` in `meta/creatives.ts`, which reads `/{videoId}/thumbnails` and prefers the `is_preferred` entry) and sends it as `video_data.image_url`. Geo targeting that includes Singapore (`SG`) or Taiwan (`TW`) requires `regional_regulated_categories` (`MetaAdSetPayload.regional_regulated_categories: string[]`) — confirmed live 2026-07-27 when a Worldwide-targeted ad set (see Country Groups' Worldwide option) was rejected with "No Taiwan Universal Ads Declaration" (error_subcode 3858498; Singapore's equivalent is error_subcode 3858550). The orchestrator computes this automatically: `REGIONAL_DECLARATION_BY_COUNTRY` maps `TW → "TAIWAN_UNIVERSAL"` / `SG → "SINGAPORE_UNIVERSAL"`, included only for an explicit custom country list that actually contains them. **Worldwide groups no longer reach this code path at all** — per a later decision, Worldwide instead auto-excludes Thailand/Singapore/Taiwan entirely (`WORLDWIDE_AUTO_EXCLUDED_COUNTRIES` in `src/lib/countries.ts`, applied in `resolveMetaGeoTargeting()` in `synthesize-campaign.ts`) rather than declaring them, so `geoExcludedCountryCodes` always contains all three for a Worldwide-linked preset and `regional_regulated_categories` naturally comes out empty. Thailand's inclusion in the auto-exclusion list is for a *different* reason than the other two: it's an age-restriction rule (under-20 audiences rejected alongside manual `publisher_platforms`, error_subcode 1870249, see below), not a `regional_regulated_categories` requirement — but the same "just don't target it" fix works for both cases, so all three are excluded together. A **custom (non-Worldwide) country list that explicitly includes Thailand** still needs `age_min` set to at least 20 (or manual exclusion) when `publisher_platforms` is used — that part of the fix is unchanged. Ad set payload always sends `attribution_spec: [{ event_type: "CLICK_THROUGH", window_days: 1 }]` unconditionally (no preset/UI toggle) — confirmed live 2026-07-15 via GET /api/meta/adsets that this is already this account's prevailing value across nearly all ad sets (a few legacy ones instead carry a 3-entry spec: 7-day click + 1-day view-through + 1-day engaged-video-view), so this hardcodes the desired 1-day-click-only setting rather than leaving it to Meta's default.
│   ├── synthesize-campaign.ts         # Converts CampaignBuildItem → platform-specific shape; synthesizeCampaign() for Snap, synthesizeMetaCampaign(…, resolvedPageId?) for Meta — looks up getMetaMediaRef(asset, adAccountId) per creative and carries a cached imageHash/videoId onto metaImageHash/metaVideoId so launch reuses Silo "→ Meta" pre-uploads instead of re-uploading; pickBestPage(allowedPageIds, runningByPage) → page with most ads remaining (fixed 250 limit for every page); resolveGeoCountryCodes(preset, fallback) → resolves a linked Country Group's current countryCodes at build time (live link — see Country Groups note below), called by both synthesizeCampaign and synthesizeMetaCampaign
│   ├── money.ts                       # usdToMicro/microToUsd (Snapchat, ×1e6) + usdToCents/centsToUsd (Meta, ×100) — THE only place dollars convert to platform integer units. Covered by money.test.ts. Replaced four independent re-implementations; never re-inline this arithmetic
│   ├── pageConfigs.ts                 # Facebook Page config CRUD (localStorage + KV sync, key: boilerroom_page_configs_v1 / br_page_configs_v1) — loadPageConfigs()/upsertPageConfig()/getPageConfig()
│   ├── resolve-campaign-name.ts       # resolveCampaignName(fallbackTemplate, item, ctx, providerTemplate?) — uses provider's NamingSegment[] template if present, else string-replace fallback; also exports generateUniqueId4()
│   ├── uploadMediaToSnapchat.ts       # Client-side upload pipeline + uploadBlobToSnapchat (server-side path for Silo uploads)
│   ├── uploadMediaToMeta.ts           # uploadBlobToMeta(blobUrl, fileName, adAccountId, mediaType) — single POST to /api/meta/media, returns {imageHash} or {videoId}; no client-side chunking/polling (the route handles it)
│   ├── silo.ts                        # Silo asset CRUD (localStorage + KV sync, key: boilerroom_silo_v1); getSnapMediaId/updateSnapchatUpload + getMetaMediaRef/updateMetaUpload (parallel per-account cache helpers for snapchatUploads[]/metaUploads[]); loadAssets() backfills metaUploads: [] for pre-existing assets
│   ├── silo-tags.ts                   # Tag CRUD + auto-naming (localStorage + KV sync, key: boilerroom_silo_tags_v1)
│   ├── silo-utils.ts                  # Browser utils: hash, optimizeImage, generateThumbnail, getVideoDuration
│   ├── presets.ts                     # Preset CRUD (localStorage + KV sync, key: boilerroom_presets_v1) — loadPresets() defaults trafficSource="snap", backfills "UK"→"GB" in geoCountryCodes; duplicatePreset(id) copies with new id/name
│   ├── country-groups.ts              # CountryGroup CRUD (localStorage + KV sync, key: boilerroom_country_groups_v1 / br_country_groups) — mirrors pixels.ts; countPresetsUsingGroup()/unlinkPresetsFromGroup() for safe deletion; backfills "UK"→"GB"
│   ├── countries.ts                   # COUNTRY_OPTIONS — shared {value,label} ISO2 country list used by PresetForm's Geo Targeting picker and CountryGroupsModal
│   ├── pixels.ts                      # Snap Pixel CRUD (localStorage + KV sync, key: boilerroom_pixels_v1)
│   ├── meta-pixels.ts                 # Meta Pixel CRUD (localStorage + KV sync, key: boilerroom_meta_pixels_v1 / br_meta_pixels)
│   ├── feed-providers.ts              # FeedProvider CRUD (localStorage + KV sync, key: boilerroom_feed_providers_v1) — upcast() normalises legacy records; getFeedProviderBadgeClasses(name) returns Tailwind classes for provider chip coloring (Vizymo=orange, Predicto=blue, fallback=cyan); getFeedProviderWashClasses(name) is the background-only sibling (same hue table, no text color) for tinting larger surfaces like a card header
│   ├── articles.ts                    # Article CRUD (localStorage + KV sync, key: boilerroom_articles_v1) — upcast() defaults query: "" for old records, trafficSources: ["Snap","Meta"] for old records
│   ├── campaign-changelog.ts          # Change log for inline budget/bid/status edits (localStorage + KV sync, key: br_campaign_changelog, max 500 entries); ChangeLogEntry: { id, squadId, field, oldValue, newValue, timestamp }; addChangeEntry() prepends + trims; getEntriesForSquad(squadId) filters newest-first; called by PerformanceTable and DrilldownModal save handlers
│   ├── build-log.ts                   # Build session log (localStorage + KV sync, key: br_build_log, max 200 sessions); loadBuildLog(), appendSession(), updateSquadInLog(), clearBuildLog(); called by WizardShell after each launch
│   ├── reporting/
│   │   └── sync-logic.ts              # syncAccount(adAccountId, startDate, endDate, timezone, accessToken?, force=false, forceStatsOnly=false) — Snapchat+Visymo+Predicto sync logic; syncs ALL ad squads (Active + Paused); Visymo uses shouldSkipFeed(:15), Predicto shouldSkipFeed(:46) — bypassable by force=true for historical dates (< yesterday); squad stats fetched via withConcurrency(adSquads, 5); syncMetaAccount(adAccountId, startDate, endDate, accessToken, force=false, forceStatsOnly=false) — Meta Insights API sync; fetches ad sets via getAdSetsByAccount, account-level insights with level=adset breakdown, upserts into meta_ad_set_stats; purchases extracted from actions array (purchase + offsite_conversion.fb_pixel_purchase), revenue from action_values; spend converted from decimal string to cents; exports dateRange(), buildRanges(), SyncResult, MetaSyncResult. **`forceStatsOnly` (added 2026-07-27)** bypasses ONLY the per-account Snapchat/Meta stats cooldown (`shouldSkip`, a flat 60-min-since-last-attempt check) — NOT the Visymo/Predicto/Predicto-FB feed-level `shouldSkipFeed` :15/:46 window gates. `force` alone still bypasses everything (dashboard "Force Refresh" / page-mount auto-heal semantics, unchanged). `cron-sync/route.ts` now passes `force=false, forceStatsOnly=true` so it can still re-check every synced account's own stats on every tick without also defeating the feed-level throttle — previously it passed `force=true`, which silently made Visymo/Predicto/Predicto FB all refresh on every tick instead of their intended alternating :15/:46 cadence.
│   ├── kv-sync.ts                     # hydrateFromKV(key) + syncToKV(key, data) — debounced 1.5s writes to /api/data
│   ├── blob-host.ts                   # isOwnBlobUrl(url) — single source of truth for blob host pinning. Parses the URL and requires OUR store (storeId derived from BLOB_READ_WRITE_TOKEN), not just any *.vercel-storage.com. Used by meta/media, silo/transcode, snapchat/media/upload-from-blob, silo/delete, catalogue/delete
│   ├── date-utils.ts                  # localTodayStr/toLocalDateStr/localDaysAgoStr/shiftDateStr — the reporting UI's date defaults read the VIEWER's calendar. `new Date().toISOString().slice(0,10)` is the UTC date: west of UTC it rolls over early, so at 18:00 Jul 29 US Pacific the dashboard defaulted to Jul 30 and rendered an empty day. Server-side code that intentionally works in UTC (cron windows, Snapchat stats math) must NOT switch to these
│   ├── app-url.ts                     # getAppUrl() — the app's own origin for OAuth redirect_uri and post-auth redirects; throws in production instead of falling back to localhost (see docs/security.md)
│   ├── api/validation-error.ts        # invalidRequest(zodError, status?, logLabel?) — uniform 422/400 that returns a compact issue list and logs the full flatten() server-side
│   ├── meta/graph-version.ts          # GRAPH_VERSION / GRAPH_BASE / OAUTH_DIALOG — the single Graph API version pin (v25.0)
│   ├── channel-status-sync.ts         # syncChannelPausedStatus(googleUserId, { snapAccessToken?, metaAccessToken? }) — **routes every channel by traffic_source**: ad_squad_snap_id holds a Snap squad UUID for Snap channels and a numeric Graph ad set id for Meta ones, so asking the wrong platform yields a 404 that the not-found branch reads as "entity deleted". Two passes: (1) status poll — Snap via getAdSquad (inactive if status !== "ACTIVE" OR effective_status present and !== "ACTIVE"), Meta via getAdSet (inactive only for the explicit META_INACTIVE_EFFECTIVE_STATUSES set: PAUSED/DELETED/ARCHIVED/CAMPAIGN_PAUSED/ADSET_PAUSED/DISAPPROVED — transitional states IN_PROCESS/PENDING_REVIEW/WITH_ISSUES count as live, because a false pause releases an earning channel while a false active only delays reuse); entity-not-found stamps paused_since, anything else increments errors (isEntityNotFound / isMetaEntityNotFound); (2) backfill pass — lists the campaign's children on the owning platform and calls pickOwningAdSet(), which requires a **unique ACTIVE** name match and returns null on a tie. Channels whose platform token is absent are counted in `skipped`, never treated as missing entities. Returns ChannelSyncResult {checked, paused, resumed, errors, backfilled, skipped}; called by cron-sync per user
│   ├── db/
│   │   ├── index.ts                   # sql helper + runMigrations() + channel CRUD: normalizeChannelStatuses(), assignChannel(), releaseChannel(), forceChannelStatus(), bulkForceChannelStatus(feedProviderId, googleUserId, newStatus) — moves all in-use channels for a provider in one UPDATE, listChannels(), bulkInsertChannels(), deleteChannels(), getInUseChannelsByUser(), getInUseChannelsWithoutSquadId() — returns in-use channels with campaign_snap_id but no ad_squad_snap_id (used by backfill), updateChannelPausedStatus(), updateChannelAdSquadId() + Snap token CRUD: upsertUserToken(), updateAdAccountIds(), getAllUserTokens(), deleteUserToken() + Meta token CRUD stubs: upsertUserMetaToken(), updateMetaAdAccountIds(), getAllUserMetaTokens(), deleteUserMetaToken() (ready for future Meta OAuth routes)
│   │   ├── token-crypto.ts            # AES-256-GCM encrypt/decrypt for stored Snapchat refresh + Meta access tokens. Key is TOKEN_ENCRYPTION_KEY (SEC-15), with a legacy SESSION_SECRET-derived key still read for pre-backfill rows — see docs/security.md for the two formats and the ordering hazard. Exports encryptToken/decryptToken/isV2Encrypted/encryptTokenLegacy (the last is rollback-only, never on the request path) + verifyCronSecret() using timingSafeEqual
│   │   ├── user-metadata.ts           # readUserMetadata/writeUserMetadata + GLOBAL_OWNER — the ONLY accessor for the `user_metadata` config store (SEC-8; replaced the public-read blob JSON store). DB-first read with a self-healing one-time legacy blob fallback; writes go to Postgres only
│   │   └── migrations.sql             # CREATE TABLE IF NOT EXISTS for all 10 tables (visymo_report + predicto_report + predicto_fb_report + report_sync_log + snapchat_ad_squad_stats + feed_provider_channels + user_snapchat_tokens + user_meta_tokens + meta_ad_set_stats + user_metadata). feed_provider_channels: traffic_source column now used (per-source pool); unique constraint v2 = (channel_id, feed_provider_id, google_user_id, traffic_source); constraint swap + index in runMigrations()
│   ├── country-map.ts                 # countryNameToCode / countryCodeToName — normalises Visymo country_name ↔ Snapchat ISO-2
│   ├── fx-rate.ts                     # getEurToUsd() — fetches frankfurter.app, cached 1h in module memory
│   ├── visymo.ts                      # fetchVisymoReport(startDate, endDate) — paginated Visymo /report/ client (partner hostname/env var name unchanged: partnerhub-api.kingsroad.io, KINGSROAD_API_TOKEN)
│   ├── predicto.ts                    # fetchPredictoWithToken(label, token, start, end) core + fetchPredictoReport (PREDICTO_API_TOKEN, Snap traffic) + fetchPredictoFbReport (PREDICTO_FB_API_TOKEN, Facebook traffic); flat, USD, skips gracefully if token unset; API returns revenue as `estimated_revenue` and metrics as strings — both coerced in the mapper
│   ├── session.ts                     # iron-session helpers & auth validation; isMetaConnected(), isMetaAdAccountAllowed() used by all Meta API routes
│   └── rate-limiter.ts                # rateLimitedCall (token bucket, max 10 req/s; a non-numeric SNAPCHAT_RATE_LIMIT_RPS now falls back to 10 instead of making INTERVAL_MS NaN and disabling throttling entirely) + rateLimitedFetch (wraps rateLimitedCall + 429 retry w/ exponential backoff: 2s/4s/8s/16s, 4 retries). All direct Snapchat API calls (including upload-from-blob) use rateLimitedFetch for automatic 429 retry. **The token bucket is per-serverless-invocation, not global/shared across invocations** — if many `/api/reporting/sync` requests run concurrently (one per ad account), each gets its own independent 10 req/s allowance, so the true aggregate rate against Snapchat can far exceed the real per-app limit and trigger genuine 429s. The dashboard mitigates this by capping concurrent account syncs (see `ACCOUNT_SYNC_CONCURRENCY` in `performance/page.tsx`) rather than fixing the limiter itself — a fully global limiter would need a shared store (e.g. Vercel KV), not yet implemented.
└── types/
    ├── wizard.ts                      # CampaignFormData, AdSquadFormData, CreativeFormData, SubmissionResults, CreativeGroup, CanvasEdges, CampaignBuildItem
    ├── feed-provider.ts               # FeedProvider (full type with snapConfig, urlConfig, channelConfig, domains, metaConfig), UrlParameter, FeedProviderDomain, ChannelSetupType
    ├── article.ts                     # Article (id, feedProviderId, slug, query, allowedHeadlines, trafficSources, createdAt)
    ├── preset.ts                      # CampaignPreset (includes trafficSource, feedProviderId, countryGroupId?, creativeDefaults, metaAdSet?)
    ├── country-group.ts               # CountryGroup { id, name, countryCodes, createdAt }
    ├── snapchat.ts                    # API payload types (SnapCampaignPayload, etc.)
    ├── meta.ts                        # Meta Graph API types: MetaCampaignPayload, MetaAdSetPayload, MetaAdCreativePayload, MetaAdPayload, MetaTargeting, MetaObjectStorySpec, MetaInsightsRow (includes adset_id, adset_name, action_values for level=adset breakdowns), MetaAdAccount, enums (MetaOptimizationGoal, MetaBillingEvent, MetaPixelEvent, etc.)
    ├── ad-account.ts                  # AdAccountConfig (id, name, hidden, feedProviderIds, platform?: "snap" | "meta")
    ├── page-config.ts                 # PageConfig (id, name, hidden, feedProviderIds) for Facebook Pages table; exports DEFAULT_PAGE_AD_LIMIT=250 (fixed, not customizable)
    ├── silo.ts                        # SiloAsset (incl. metaUploads[]), SiloTag, SnapchatUploadStatus/Stage, MetaUploadStatus/Stage
    ├── pixel.ts                       # SavedPixel type
    ├── meta-pixel.ts                  # SavedMetaPixel type
    └── session.ts
```

